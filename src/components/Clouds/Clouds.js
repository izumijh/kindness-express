import React from "react";

// import React Bootstrap components
import Row from "react-bootstrap/Row";

// import css modules
import classes from "./Clouds.module.css";

const Clouds = (props) => {
  return <Row className={classes.cloudsContainer}></Row>;
};

export default Clouds;
